spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Global_Banking
    username: postgres
    password: Vigneshg@29
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          current_session_context_class: thread